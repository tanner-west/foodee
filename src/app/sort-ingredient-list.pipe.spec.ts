import { SortIngredientListPipe } from './sort-ingredient-list.pipe';

describe('SortIngredientListPipe', () => {
  it('create an instance', () => {
    const pipe = new SortIngredientListPipe();
    expect(pipe).toBeTruthy();
  });
});
